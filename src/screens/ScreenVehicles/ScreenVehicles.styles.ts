import styled from 'styled-components/native';
import {Dimensions} from 'react-native';

export const VehieclesContainer = styled.View`
  width: ${Dimensions.get('window').width}px;
  height: 100%;
`;
